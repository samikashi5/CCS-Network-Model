# Parameters To Finalize

This file tracks what is still missing before results can be treated as final.

## Financial Inputs (NEED INFO)

- discount_rate = NEED INFO
- asset_lifetime_years = NEED INFO

## Transport And Storage Inputs (NEED INFO)

- trucking_cost_usd_per_ton_km = NEED INFO
- pipeline_opex_usd_per_ton_km = NEED INFO
- pipeline_capex_usd_per_ton_km = NEED INFO
- storage_cost_usd_per_ton = NEED INFO

## Capacity Inputs (NEED INFO)

- sink_capacity_tpy = 2,000,000 (confirm this is fixed)
- capture_target_tpy = NEED INFO (optional target flow)

## Literature Evidence Log

Record the source for each value you set:

1. Parameter name:
2. Value used:
3. Unit:
4. Citation/source:
5. Why this value was chosen:

## California Truck Inputs To Collect

- average truck speed relevant to source-to-sink trips
- fuel cost basis and date
- labor/driver rate basis

## Pipeline Inputs To Collect

- route-adjusted cost assumptions for overground vs underground alignment
- capital and operating assumptions used to derive per ton-km costs
